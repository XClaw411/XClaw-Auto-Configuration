# 🖼️ 生成论文图

AI 驱动的学术论文图表生成功能模块。

## 安装前依赖

本模块依赖 `poyo-nano-banana-2` 模型，请先安装：

```
clawhub install poyo-nano-banana-2 --force
```

或在 ClawHub 中搜索安装 `poyo-nano-banana-2`。

## 环境变量

在项目根目录 `.env` 中配置：

```
POYO_API_KEY=你的密钥
```
